apiVersion: apps/v1
kind: Deployment
metadata:
  name: dep-fast
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fast
  template:
    metadata:
      labels:
        app: fast
    spec:
      containers:
      - name: con-fast
        image: <ECR레포주소>/fast:1

---
apiVersion: v1
kind: Service
metadata:
  name: svc-fast
spec:
  selector:
    app: fast
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
